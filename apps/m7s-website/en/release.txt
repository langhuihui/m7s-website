# v4.4.1
* 9cb7010 1. Add SErepublish event to indicate successful republishing, distinguishing it from the first successful SEPublish event. 2. Fix ReConnectCount not incrementing during push. 3. Use v0.32.0 version of github.com/quic-go/quic-go. 4. Fix issue where subscription mode > 1 does not start from an IDR frame. 5. Fix issue where MessageStreamID is not set when pushing with the RTMP plugin. 6. Upgrade aler9/gortsplib third-party dependency to v2.1.0 and make it adaptable. 7. Save state change history information in Stream.

# v4.4.0
* 8d5c6d6 Add time backwards function, subscription mode parameters, memory recycle mechanism, time length configuration, fix no audio in first screen rendering, fix rtmp push and pull without parameters, fix DTS generation error caused by PTS exceeding maximum value, fix rtmp parsing issue with extended timestamps, fix several null pointer panic issues.

# v4.3.10
* 3ead36c Fix one engine performance issue and add a switch for first screen rendering.

# v4.3.9
* 3ead36c Fix one engine performance issue and add a switch for first screen rendering.

# v4.3.8
* a4e1f49 1. Optimize Track data writing process for clarity and readability. 2. Add 3 configuration items for Subscribe to specify parameter names when subscribing to specific tracks. 3. Fix issue where AbsTime starts from 0 on first write. 4. Fix issue where audio data is read too late during subscription. 5. Fix issue where remote connection is not closed when RTMP and RTSP plugins push or pull. 6. Upgrade aler9/gortsplib third-party dependency to v2.1.0 and make it adaptable. 7. Save state change history information in Stream.

# v4.3.7
* 53cc788 1. Put amf0 encoder and decoder into codec/amf.go. 2. Check length when writing avcc audio. 3. Set recorder as internal subscriber. 4. Fix initialization error in edge plugin for remote HDL pulling. 5. Add MallocSlice function to reduce memory copying. 6. Modify flv header after recording to add key frame metadata for drag and drop progress bar playback.

# v4.3.6
* 6b40297 1. Smooth out remote HLS stream pulling to solve stuttering problems. 2. Add a null pointer check in stream pulling logic. 3. Set the speedlimit default value to 500. 4. Set subscriber of snap plugin as internal subscriber to prevent automatic closure. 5. Do not read ssrc value for TCP port range of gb28181 plugin.

# v4.3.5
* f8e50d0 Comment out one println statement.

# v4.3.4
* 361b052 1. Fix serialization cycle reference issue in Stream. 2. Implement support for SubVideoTracks and SubAudioTracks in subscribe. 3. Fix flush issue when reading aac data in ts. 4. Fix an error in IsClosed object call when subscribing ends. 5. Add DefaultYaml capability to solve the issue of plugin default configuration being overridden by global configuration. 6. Fix parameter transmission issue in rtsp plugin during subscription. 7. Set snap and record plugins to use DefaultYaml default configuration.

# v4.3.3
* 27784f0 1. Fix bug where delayclosetimeout value is overridden. 2. Refactor subscriber collection in stream to reduce ineffective wait time. 3. Implement internal subscriber function to prevent HLS plugin from affecting the last subscriber exit logic. 4. Breaking change: change the pullonstart type in pull configuration to map and change pullonsubscribe to pullonsub and change the type to map. 5. Add support for rtmps protocol in rtmp plugin and optimize handshake process. 6. Fix timestamp handling issue in gb28181 plugin (causes skipping seconds and other issues). 7. Optimize reconnection mechanism for hdl plugin to stop reconnecting on server responses other than 200.

# v4.3.2
* 34394b9 1. Add timeout parameter in http configuration to set http response timeout. 2. Add custom middleware functionality in http configuration. 3. Add timeout handling in hdl and jessica plugins.

# v4.3.0
* 7138565 Upgrade engine to 4.9.0 and update hls, hook, record, and room plugins.

# v4.2.9
* a521c63 Fix bug where the last subscriber leaves and the edge plugin can only pull one stream.

# v4.2.8
* ed5e719 Fix decoding of h265 encoding format in remote http-flv stream.

# v4.2.7
* 664a64a Fix caching and make it compatible with webrtc.

# v4.2.6
* f097e66 Fix issues in GB, including the problem of automatically generated DTS being overridden.

# v4.2.5
* 2e8d4d2 Add support for playing fMP4 format live streams.

# v4.2.4
* 8bb63c9 1. Set first timeout to follow PublishTimeout configuration. 2. Split large AAC audio packets during RTP packetizing. 3. Default mute audio in snapshot plugin to improve speed. 4. Improve PS packet parsing compatibility in GB plugin. 5. Upgrade gortsplib version in RTSP plugin. 6. Fix stream path setting bug in RTMP plugin for external push and pull.
# v4.2.2
* c0b2a10 👌 IMPROVE: added h265 and G711 support for HLS, added AAC support for Gb28181


# v4.2.1
* 1a57aeb 🐛 FIX: fixed RTMP handshake protocol and SEI frame processing


# v4.2.0
* 85b134d 👌 IMPROVE: added throttling setting, fixed audio timestamp unit for Gb28181 with single port


# v4.1.11
* 9969966 🐛 FIX: fixed incorrect judgment when inferring video encoding format for Gb28181


# v4.1.10
* 9969966 🐛 FIX: fixed incorrect judgment when inferring video encoding format for Gb28181


# v4.1.9
* 5482ab4 👌 IMPROVE: more advanced RTP packet out-of-order reordering algorithm


# v4.1.8
* 61eaaab 🐛 FIX: prevented GB plugin from automatically generating DTS


# v4.1.7
* 54f01a0 🐛 FIX: fixed console modification of configuration file


# v4.1.6
* 09570a6 🐛 FIX: fixed recording function


# v4.1.5
* 00ff25e 🐛 FIX: compatible with go1.19 and m1


# v4.1.4
* 33b5c46 🐛 FIX: fixed out-of-bounds access issue when AAC packet is too small


# v4.1.3
* 2b1bf24 🐛 FIX: fixed bug where logrotate plugin failed to compile on Windows


# v4.1.0
* d05bd7b 📦 NEW: added edge plugin to provide simple cascading function


# v4.0.13
* 6f9fe7c 🐛 FIX: fixed problem with RTMP plugin and SRS server communication


# v4.0.12
* 96d886c 🐛 FIX: fixed bug in hook plugin's configuration file reading


# v4.0.11
* ca2f695 🐛 FIX: fixed signaling splicing error in GB pull stream


# v4.0.10
* 0fd608a 🐛 FIX: fixed the problem of subscription keyframe being overridden in the engine, the problem of sending deviceID value in the ptzcmd of gb, and the parsing problem of rtp packets exceeding one MTU in AAC


# v4.0.9
* d5e6a51 🐛 FIX: fixed DTS generation mechanism when there is no B frame, added device information persistence to GB plugin


# v4.0.8
* e035cf0 👌 IMPROVE: added error message for RTSP pull stream, added PS stream dump function to GB plugin for easy error diagnosis


# v4.0.7
* f82c423 🚀 RELEASE: fixed the problem of concurrent remote stream storage, fixed the PS packet parsing bug for GB pull stream, and exported some plugins


# v4.0.6
* 87f0806 API added the ability to get information about the streams imported from a remote endpoint and the streams pushed to a remote endpoint


# v4.0.5
* da69aae FIX: fixed the bug of pts and dts overwrite in GB plugin, and made rtsp protocol compatible with sdp without sps and pps


# v4.0.4
* 6e8081a 1. added configuration log level; 2. added configurable disconnection delay exit time configuration for publisher


# v4.0.3
* 70b5c55 FIX: fixed the problem of incorrect reading caused by the release of data track, and the problem of DTS generation when writing in annexb format


# v4.0.2
* 4486c61 added logic for null judgment in the RTMP protocol data writing process


# v4.0.1
* 29f574a added 10s fps and bps history statistics, added the forced protocol specification configuration for RTSP plugin during pull stream, fixed several bugs


# v4.0.0
* d12052e fixed the compatibility of SSRC value inferred by gb28181 plugin and IP acquisition


# v4.0.0
* 9234c10 fixed the compatibility of SSRC value inferred by gb28181 plugin and IP acquisition


# v4.0.0-beta9
* 7a0964b Merged branch 'v4' of github.com:langhuihui/monibuca into v4